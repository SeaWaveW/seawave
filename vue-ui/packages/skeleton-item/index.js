import SwSkeletonItem from '../skeleton/src/item';

/* istanbul ignore next */
SwSkeletonItem.install = function(Vue) {
  Vue.component(SwSkeletonItem.name, SwSkeletonItem);
};

export default SwSkeletonItem;
