import SwInputNumber from './src/input-number';

/* istanbul ignore next */
SwInputNumber.install = function(Vue) {
  Vue.component(SwInputNumber.name, SwInputNumber);
};

export default SwInputNumber;
