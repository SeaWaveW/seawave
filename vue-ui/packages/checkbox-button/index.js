import SwCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
SwCheckboxButton.install = function(Vue) {
  Vue.component(SwCheckboxButton.name, SwCheckboxButton);
};

export default SwCheckboxButton;
