import SwButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
SwButtonGroup.install = function(Vue) {
  Vue.component(SwButtonGroup.name, SwButtonGroup);
};

export default SwButtonGroup;
